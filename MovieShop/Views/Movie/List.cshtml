@model List<List<MovieModel>>

<table style="margin-left: auto; margin-right: auto;">
    @foreach(var p in Model)
    {
        <tr>
            @foreach (var e in p)
            {
                <td>
                    <a href="google.com">
                        <img src="@e.PosterUrL" style="width: 200px; height: 250px; margin: 10px;">
                    </a>
                </td>
            }
        </tr>
    }
</table>

<script>
</script>
